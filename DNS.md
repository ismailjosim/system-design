# Domain Name System (DNS): ইন্টারনেটের ফোনবুক ও এর কার্যপ্রণালী

আমরা যখন ব্রাউজারে `google.com` বা `facebook.com` লিখি, তখন আমাদের কম্পিউটার কিন্তু সরাসরি এই নামগুলো বোঝে না। কম্পিউটার বা সার্ভারগুলো ইন্টারনেটে একে অপরের সাথে যোগাযোগ করে **IP Address** (যেমন: `142.250.190.46`) দিয়ে।

মানুষের পক্ষে অসংখ্য ওয়েবসাইটের আইপি এড্রেস মনে রাখা অসম্ভব। এই সমস্যার সমাধান করে **DNS** (Domain Name System)। এটিকে বলা হয় **"ইন্টারনেটের ফোনবুক"** (Phonebook of the Internet)। এর কাজ হলো মানুষের পাঠযোগ্য ডোমেইন নেমকে (Domain Name) কম্পিউটারের পাঠযোগ্য IP Address-এ রূপান্তর করা।

---

## 🔍 কীভাবে কাজ করে DNS Resolution Process? (step-by-step)

যখন আপনি ব্রাউজারে `[https://systemdesignbangla.com](https://systemdesignbangla.com)` টাইপ করে **Enter** চাপেন, তখন ব্যাকএন্ডে একটি অনুসন্ধানী প্রক্রিয়া শুরু হয়। একে বলা হয় **DNS Lookup** বা **DNS Resolution**। পুরো প্রক্রিয়াটি ৮টি ধাপে সম্পন্ন হয়:

```
[Browser] ➔ (1) Query ➔ [DNS Resolver / ISP]
                           │
                           ├── (2) Ask ➔ [Root Server (.)]
                           ├── (3) Return TLD Server IP ↵
                           │
                           ├── (4) Ask ➔ [TLD Server (.com)]
                           ├── (5) Return Authoritative Server IP ↵
                           │
                           ├── (6) Ask ➔ [Authoritative Server]
                           └── (7) Return Final IP (e.g. 104.21.5.12) ↵
                           │
[Browser] ⬅ (8) Connect to IP ⬅ [DNS Resolver]

```

1. **User Request:** আপনি ব্রাউজারে `systemdesignbangla.com` লিখলেন।
2. **Checking Cache:** প্রথমে আপনার ব্রাউজার, অপারেটিং সিস্টেম এবং রাউটারের নিজস্ব ক্যাশ (Cache) চেক করা হয়। যদি আগে থেকে ক্যাশে আইপি এড্রেস সেভ করা থাকে, তবে সেখান থেকেই রেসপন্স চলে আসে।
3. **DNS Recursive Resolver:** ক্যাশে না থাকলে রিকোয়েস্ট যায় আপনার ISP (Internet Service Provider) বা পাবলিক ডেনএস রিকিউরসিভ রিজলভারের কাছে (যেমন: Cloudflare `1.1.1.1` বা Google `8.8.8.8`)।
4. **Root Name Server (.):** রিজলভার প্রথমে রুট সার্ভারকে জিজ্ঞেস করে। রুট সার্ভার পুরো আইপি জানে না, কিন্তু সে `.com` TLD কোথায় আছে তার এড্রেস রিজলভারকে দেয়।
5. **TLD Name Server (.com):** রিজলভার তখন `.com` Top-Level Domain সার্ভারের কাছে যায়। TLD সার্ভার ডোমেনের আসল সার্ভারের সন্ধান দেয়।
6. **Authoritative Name Server:** এটি মূল সার্ভার যা নির্দিষ্ট ওই ডোমেইনের আসল IP Address সংরক্ষণ করে। এটি রিজলভারকে মূল IP Address প্রদান করে।
7. **IP Delivery & Caching:** রিজলভার আইপিটি ব্রাউজারকে দেয় এবং ভবিষ্যতে দ্রুত ব্যবহারের জন্য ক্যাশে সেভ করে রাখে।
8. **HTTP Request:** ব্রাউজার সরাসরি ওই আইপি এড্রেসে সংযোগ স্থাপন করে পেজ লোড করে।

---

## 🏗️ DNS Hierarchy (ডোমেইনের গঠন)

একটি সম্পূর্ণ ডোমেইন নেম (FQDN) হাইরার্কিকাল বা গাছের কাঠামোর মতো সাজানো থাকে। ডান দিক থেকে বাঁ দিকে এটি বিশ্লেষণ করা হয়:

`[www.systemdesignbangla.com](https://www.systemdesignbangla.com).`

- **Root Zone (`.`):** একদম শেষে একটি অদৃশ্য ডট (`.`) থাকে, যা রুট লেভেলকে নির্দেশ করে।
- **Top-Level Domain (TLD):** ডোমেইনের শেষের অংশ। যেমন: `.com`, `.org`, `.net`, বা কান্ট্রি কোড TLD যেমন `.bd`।
- **Second-Level Domain (SLD):** মূল ব্র্যান্ডের নাম। যেমন: `systemdesignbangla` বা `google`।
- **Subdomain:** মূল ডোমেইনের অধীনে থাকা নির্দিষ্ট অংশ। যেমন: `www`, `api`, বা `blog`।

---

## 📋 প্রধান প্রধান DNS Records

একটি ডোমেইন কোন সার্ভারে কিভাবে নির্দেশ করবে, তা সেট করার জন্য **DNS Records** ব্যবহার করা হয়:

| Record Type     | কাজ / বিবরণ                                                                          | উদাহরণ                                               |
| --------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------- |
| **A Record**    | ডোমেইনকে একটি IPv4 এড্রেসের সাথে ম্যাপ করে।                                          | `example.com` ➔ `93.184.216.34`                      |
| **AAAA Record** | ডোমেইনকে একটি IPv6 এড্রেসের সাথে ম্যাপ করে।                                          | `example.com` ➔ `2606:2800:220:1:248:1893:25c8:1946` |
| **CNAME**       | একটি ডোমেইনকে অন্য একটি ডোমেইনের এলিয়াস (Alias) বা শর্টকাট হিসেবে পয়েন্ট করে।        | `blog.example.com` ➔ `hashnode.network`              |
| **MX Record**   | ইমেইল পাঠানোর জন্য ব্যবহৃত মেল সার্ভার (Mail Server) নির্ধারণ করে।                   | `example.com` ➔ `mail.google.com`                    |
| **TXT Record**  | ডোমেইনের তথ্য এবং সিকিউরিটি ভেরিফিকেশনের (SPF, DKIM, SSL) জন্য টেক্সট ডেটা ধারণ করে। | `v=spf1 include:_spf.google.com ~all`                |
| **NS Record**   | ডোমেইনের authoritative nameservers নির্দেশ করে।                                      | `ns1.cloudflare.com`                                 |

---

## ⚡ DNS Caching & TTL (Time To Live)

বারবার রুট বা TLD সার্ভারে রিকোয়েস্ট পাঠালে ইন্টারনেটের গতি ধীর হয়ে যাবে। তাই **DNS Caching** ব্যবহার করা হয়।

- **TTL (Time to Live):** প্রতিটি DNS রেকর্ডেই একটি সময়সীমা নির্ধারণ করা থাকে (যেমন: ৩৬০০ সেকেন্ড বা ১ ঘণ্টা)। এই সময়সীমাকে TTL বলে।
- **ক্যাশিং স্তর:** ব্রাউজার ➔ ওএস (OS) ➔ রাউটার ➔ আইএসপি (ISP Resolver) — এই প্রতিটি লেভেলেই DNS রেজাল্ট ক্যাশ হয়ে থাকে। TTL শেষ হলে আবার নতুন করে DNS Lookup করা হয়।

---

## 🎯 সিস্টেম ডিজাইনে DNS-এর ভূমিকা

সিস্টেম আর্কিটেক্টদের জন্য DNS শুধু একটি ফোনবুক নয়, বরং স্কেলেবিলিটি ও অ্যাভেইলেবিলিটি নিশ্চিত করার একটি বড় হাতিয়ার:

1. **DNS-Based Load Balancing (Round Robin DNS):** একটি ডোমেইনের একাধিক A Record (একাধিক IP) যুক্ত করে ট্র্যাফিক বিভিন্ন সার্ভারে সমানভাবে ভাগ করে দেওয়া যায়।
2. **GeoDNS / Geo-Routing:** ইউজারের ভৌগোলিক অবস্থানের ওপর ভিত্তি করে সবচেয়ে কাছের ডেটাসেন্টারের IP রিটার্ন করা যায় (যেমন: এশিয়ার ইউজারকে সিঙ্গাপুর সার্ভারের IP দেওয়া)।
3. **High Availability & Failover:** কোনো একটি ডাটা সেন্টার ডাউন হয়ে গেলে হেলথ চেকের মাধ্যমে স্বয়ংক্রিয়ভাবে অন্য সচল সার্ভারের IP তে ট্রাফিক পয়েন্ট করা যায়।
4. **CDN Integration:** Cloudflare বা Fastly-এর মতো CDN গুলো DNS লেভেলেই অ্যাটাক (DDoS) ব্লক করে এবং কনটেন্ট দ্রুত ডেলিভারি দেয়।

---

## ❌ সাধারণ কিছু সমস্যা ও Troubleshooting

- **DNS Propagation Delay:** ডোমেইনের রেকর্ড পরিবর্তন করলে নতুন IP পুরো বিশ্বের ক্যাশ থেকে আপডেট হতে কয়েক মিনিট থেকে ২৪-৪৮ ঘণ্টা সময় লাগতে পারে।
- **Command Line Tools:**
- `nslookup systemdesignbangla.com` — ডোমেইনের IP চেক করতে।
- `dig systemdesignbangla.com` — বিস্তারিত DNS রেকর্ড ট্রেস করতে।
